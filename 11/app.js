console.log('Starting app.js');

const fs = require('fs');
const os = require('os');
const _ = require('lodash'); // External modules usage example

//Files
//const notes = require('notes');

// Importes module usage example
    console.log(_.isString(true));
    console.log(_.isString('Seba'));
    var filteredArray = _.uniq(['Seba',1,1,'Seba',3,4,51,3,3,7]);
    console.log(filteredArray);
